# 04 — Activity / Main User Flow

## تدفق المستخدم الرئيسي

يمثل هذا المخطط التدفق التشغيلي الرئيسي لميزة إدارة الأرصدة الافتتاحية، من فتح الشاشة وإدخال الرأس وإضافة التفاصيل، مرورًا بالتعديل والحذف، وانتهاءً بعملية الحفظ والتحقق من حالات الفشل.

## المخطط

```mermaid
flowchart TD
    classDef proc fill:#e8eaff,stroke:#333,stroke-width:1px,color:#111
    classDef decide fill:#f3f0ff,stroke:#333,stroke-width:1px,color:#111
    classDef term fill:#ffffff,stroke:#333,stroke-width:1px,color:#111

    Start(["بداية"]):::term --> UC001["UC-001: فتح شاشة الأرصدة الافتتاحية"]:::proc
    UC001 --> LOAD{"هل عُملت البيانات؟  
(Products + Warehouses  
من Mock Data — FR-09)"}:::decide

    LOAD -- "نعم" --> UC002["UC-002: إدخال رأس الوثيقة  
(رقم الوثيقة، التاريخ، المستخدم، البيان)  
— FR-01/FR-02"]:::proc
    LOAD -- "لا — فشل التحميل /  
القوائم غير متوفرة" --> MSG1["عرض رسالة مناسبة  
+ عدم الانتقال للخطوة التالية  
(UC-001 Alternate Flows)"]:::proc
    MSG1 --> UC001

    UC002 --> HDR{"هل الرأس صالح؟  
— البيانات المطلوبة موجودة (BR-01)  
— تاريخ صالح (UC-002 A3)"}:::decide

    HDR -- "نعم" --> UC003["UC-003: بدء إضافة سطر تفاصيل  
(إدخال/اختيار البيانات) — FR-03"]:::proc
    HDR -- "لا" --> MSG2["رسالة خطأ مناسبة (NFR-04)  
+ منع الانتقال"]:::proc
    MSG2 --> UC002

    UC003 --> UC004["UC-004: اختيار Product  
— Products من قائمة (Mock Data)"]:::proc
    UC004 --> UC005["UC-005: اختيار Warehouse  
— Warehouses من قائمة (Mock Data)"]:::proc
    UC005 --> QUANT["إدخال Quantity (BR-02)  
+ Price إن وُجد (اختياري — 6.7)  
+ ExpiryDate إن وُجد (اختياري — 6.7)"]:::proc
    QUANT --> ADD["الضغط على Add"]:::proc
    ADD --> DT{"صالح؟ Detail هل  
— Product مطلوب (BR-05)  
— Warehouse مطلوب (BR-04)  
— Quantity > 0 (BR-02)"}:::decide

    DT -- "نعم" --> UC006["UC-006: تحديث وعرض جدول التفاصيل  
— Product Name / Warehouse Name (BR-06)  
— Quantity + Price إن وجد + ExpiryDate إن وجد"]:::proc
    DT -- "لا" --> MSG3["رسالة خطأ مناسبة (NFR-04)  
+ لا يُضاف السطر"]:::proc
    MSG3 --> UC003

    UC006 --> CHOOSE{"الإجراء التالي؟"}:::decide

    CHOOSE -- "إضافة سطر آخر" --> UC003
    CHOOSE -- "تعديل" --> UC007["UC-007: فتح بيانات السطر الحالية  
— تعديل: Product / Warehouse  
/ Quantity / Price / ExpiryDate"]:::proc
    CHOOSE -- "حذف" --> UC008["UC-008: عرض ConfirmDialog  
— Found (موجود) Confirm / Cancel  
— السجل غير موجود: A2"]:::proc
    CHOOSE -- "حفظ" --> UC009["UC-009: بدء الحفظ"]:::proc

    UC008 --> REC{"السجل موجود؟"}:::decide
    REC -- "نعم" --> CONF{"Confirm / Cancel؟"}:::decide
    CONF -- "Cancel" --> UC006
    CONF -- "Confirm" --> DEL["حذف السطر + تحديث الجدول  
+ رسالة نجاح (NFR-04)"]:::proc
    DEL --> UC006
    REC -- "لا" --> MSG5["رسالة مناسبة  
+ العودة للجدول دون تغيير (UC-008 A2)"]:::proc
    MSG5 --> UC006

    UC007 --> ED{"تعديل / إلغاء؟"}:::decide
    ED -- "إلغاء" --> UC006
    ED -- "تعديل" --> REV{"إعادة التحقق  
(نفس معايير BR-01..05)"}:::decide
    REV -- "نجاح" --> UPD["تحديث السطر + تحديث الجدول  
+ رسالة نجاح (NFR-04)"]:::proc
    REV -- "فشل" --> MSG4["رسالة خطأ + عدم تحديث البيانات"]:::proc
    UPD --> UC006
    MSG4 --> UC006

    UC009 --> A1{"A1: هل الرأس مكتمل؟"}:::decide
    A1 -- "نعم" --> A2{"A2: توجد Details؟  
(صف واحد على الأقل — UC-009 A2)"}:::decide
    A1 -- "لا" --> M1["«يرجى استكمال بيانات الرصيد الافتتاحي»  
+ لا يتم الحفظ (UC-009 A1)"]:::proc
    M1 --> UC006

    A2 -- "نعم" --> A3{"A3: صحيحة؟ كل Details  
(BR-01..BR-05)"}:::decide
    A2 -- "لا" --> M2["«يجب إضافة صنف واحد على الأقل  
قبل الحفظ» + لا يتم الحفظ (UC-009 A2)"]:::proc
    M2 --> UC006

    A3 -- "نعم" --> A4{"A4: نجاح الحفظ؟"}:::decide
    A3 -- "لا" --> M3["رسالة خطأ (NFR-04)  
+ لا يتم الحفظ (UC-009 A3)"]:::proc
    M3 --> UC006

    A4 -- "نعم" --> SAV["داخل التطبيق Header + Details حفظ  
— Mock / In-Memory (FR-09/UC-009)"]:::proc
    A4 -- "فشل الحفظ" --> M4["رسالة فشل الحفظ  
+ إمكانية إعادة المحاولة (UC-009 A4)"]:::proc
    M4 --> UC009

    SAV --> End(["نهاية"]):::term
```

---

## 1. نطاق المخطط

يغطي المخطط دورة العمل الرئيسية التالية:

```text
فتح الشاشة
    ↓
تحميل البيانات الوهمية
    ↓
إدخال رأس الوثيقة
    ↓
إضافة التفاصيل
    ↓
عرض التفاصيل
    ↓
إضافة / تعديل / حذف
    ↓
حفظ الرصيد الافتتاحي
    ↓
نجاح أو معالجة خطأ
```

---

## 2. UC-001 — فتح الشاشة

يبدأ التدفق بفتح شاشة الأرصدة الافتتاحية ثم التحقق من توفر بيانات:

- Products
- Warehouses

المصدر هو Mock Data.

عند فشل التحميل أو عدم توفر القوائم، يتم عرض رسالة مناسبة وعدم الانتقال إلى الخطوة التالية.

---

## 3. UC-002 — إدخال رأس الوثيقة

يدخل المستخدم:

- رقم الوثيقة.
- التاريخ.
- المستخدم.
- البيان.

يتم التحقق من البيانات قبل الانتقال إلى إضافة التفاصيل.

---

## 4. UC-003 / UC-004 / UC-005 — إضافة Detail

يتبع إضافة السطر التسلسل التالي:

```text
اختيار Product
      ↓
اختيار Warehouse
      ↓
إدخال Quantity
      ↓
Price عند الحاجة
      ↓
ExpiryDate عند الحاجة
      ↓
Add
      ↓
Validation
```

قواعد التحقق الظاهرة في المخطط:

- Product مطلوب.
- Warehouse مطلوب.
- Quantity > 0.

---

## 5. UC-006 — عرض التفاصيل

بعد نجاح إضافة السطر، يتم تحديث جدول التفاصيل لعرض:

- Product Name.
- Warehouse Name.
- Quantity.
- Price عند وجوده.
- ExpiryDate عند وجوده.

---

## 6. UC-007 — تعديل Detail

يمكن للمستخدم تعديل:

- Product.
- Warehouse.
- Quantity.
- Price.
- ExpiryDate.

ثم تتم إعادة عملية التحقق قبل تحديث السجل والجدول.

---

## 7. UC-008 — حذف Detail

يتضمن الحذف:

```text
حذف
  ↓
التحقق من وجود السجل
  ↓
Confirm / Cancel
```

### Confirm

- حذف السطر.
- تحديث الجدول.
- عرض رسالة نجاح.

### Cancel

- العودة إلى الجدول.
- عدم تغيير البيانات.

### السجل غير موجود

- عرض رسالة مناسبة.
- العودة إلى الجدول دون تغيير.

---

## 8. UC-009 — الحفظ

يمر الحفظ عبر أربع حالات بديلة:

### A1 — Header غير مكتمل

لا يتم الحفظ، وتظهر رسالة:

> «يرجى استكمال بيانات الرصيد الافتتاحي»

### A2 — لا توجد Details

لا يتم الحفظ، وتظهر رسالة:

> «يجب إضافة صنف واحد على الأقل قبل الحفظ»

### A3 — تفاصيل غير صحيحة

يتم منع الحفظ مع عرض رسالة توضح المشكلة.

### A4 — فشل الحفظ

تظهر رسالة فشل الحفظ مع إمكانية إعادة المحاولة.

### النجاح

يتم حفظ:

```text
Header + Details
```

داخل التطبيق باستخدام:

```text
Mock / In-Memory Data
```

---

## 9. Traceability

| المتطلب / حالة الاستخدام | الجزء في المخطط |
|---|---|
| FR-01 / UC-001 | فتح شاشة الأرصدة الافتتاحية |
| FR-02 / UC-002 | إدخال رأس الوثيقة |
| FR-03 / UC-003 | بدء وإكمال إضافة Detail |
| FR-04 / UC-004 | اختيار Product |
| FR-05 / UC-005 | اختيار Warehouse |
| FR-06 / UC-006 | تحديث وعرض جدول التفاصيل |
| FR-07 / UC-007 | تعديل Detail |
| FR-08 / UC-008 | Confirm / Cancel ثم الحذف |
| FR-09 / UC-009 | التحقق والحفظ |

---

## 10. المراجع

يتوافق هذا التدفق مع حالات الاستخدام والمتطلبات المرتبطة بميزة الأرصدة الافتتاحية، مع الحفاظ على Mock Data / In-Memory ضمن نطاق التنفيذ.

---

## 11. موقع الملف

```text
docs/
└── design/
    └── 05-activity-main-user-flow.md
```
